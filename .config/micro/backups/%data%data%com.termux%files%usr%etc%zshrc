	/data/data/com.termux/files/usr/libexec/termux/command-not-found "$1"
}
#set nomatch so *.sh would not error if no file is available
setopt +o nomatch
. /data/data/com.termux/files/usr/etc/profile

tput civis
#mpv /sdcard/hacker.mp3 > /dev/null 2>&1
clear
echo -ne '\e[5 q'
echo "
 < ━━━━━━━━━━━━━━ [★] T E R M U X [★] ━━━━━━━━━━━━━━ >  " |lolcat
echo 
figlet -f ASCII-Shadow 'Termux' |lolcat -t
# display ipv4 address of user
myip=$(ifconfig | grep 'inet' | grep -v '::1' | grep -v '127.0.0.1' | awk NR==1'{print $2}')
echo "
 < ━━━━━━━ [★] $myip [★] ━━━━━━━ > " |lolcat
tput cnorm
# Prompt setting
PS1='%# '
preexec() { echo -ne '\e[5 q'}

