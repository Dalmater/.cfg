#!/bin/zsh
sdate=$1
if [ -z "$sdate" ];
then
  sdate="-7h -30min"
fi
export sdate
date=`$PREFIX/usr/bin/php << 'EOF'
<?php
date_default_timezone_set("Etc/GMT+2");
$date = strtotime(GETENV("sdate"));
echo "\r".$date;
EOF`
echo $sdate
echo $date
$HOME/bin/rtcwake -m mem -t $date
